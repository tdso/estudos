## Revisitando

```
Path file = Paths.get(folder_name);
BufferedReader in;
in = Files.newBufferedReader(file, StandardCharset.UTF_8);
String s;
    while (s = in.readLine != null){
list.add(s);
}
```
